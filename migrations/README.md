# Migration Infrastructure Placeholder

This directory reserves the future governed migration location.

Current status:

- no migration framework is installed;
- no executable migration exists;
- no SQL file exists;
- no schema change is authorized;
- no database target is configured;
- migration execution remains blocked.

Future migration adoption requires separate authorization, accountable owners,
deployment parity, an approved schema baseline, isolated database tests,
compatibility evidence, and rollback or forward-recovery evidence.
